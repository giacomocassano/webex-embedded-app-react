import styled from 'styled-components';

const Styles = {};

//Set the page container to be 100% of the viewport height and the background color to be white
Styles.PageContainer = styled.div`
  position: relative;
  background-color: white;
`;

export default Styles;
